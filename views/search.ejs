
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WebLens</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* Basic Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Container */
.weblenz-container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Header */
.weblenz-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #000;
    padding: 20px;
    color: #fff;
}

.weblenz-title {
    font-size: 24px;
    font-weight: bold;
}

.weblenz-search-container {
    display: flex;
    align-items: center;
}

.weblenz-search-input {
    padding: 10px;
    border: none;
    border-radius: 4px 0 0 4px;
    font-size: 16px;
}

.weblenz-search-button {
    padding: 10px;
    background-color: #ffcc00;
    border: none;
    border-radius: 0 4px 4px 0;
    cursor: pointer;
    font-weight: bold;
}

/* Cards */
.weblenz-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-top: 20px;
}

.weblenz-card {
    background-color: #333;
    color: #fff;
    padding: 20px;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
}

.weblenz-card-icon {
    background-color: #fff;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    margin-bottom: 10px;
}

.weblenz-card-title {
    font-size: 18px;
    font-weight: bold;
    margin-bottom: 10px;
}

.weblenz-card-text {
    font-size: 14px;
}

/* Footer */
.weblenz-footer {
    background-color: #000;
    color: #fff;
    text-align: center;
    padding: 20px;
    margin-top: 20px;
}

/* Responsive Design */
@media (max-width: 768px) {
    .weblenz-cards {
        grid-template-columns: 1fr;
    }
}

    </style>
</head>
<body>
    <div class="weblenz-container">
        <header class="weblenz-header">
            <h1 class="weblenz-title">WEBLENS</h1>
            <div class="weblenz-search-container">
                <form action="/search" method="post">
                <input type="text" class="weblenz-search-input" name="query" placeholder="Search...">
                <button class="weblenz-search-button">Find</button>
                </form>
            </div>
        </header>
        <div class="weblenz-cards">
            <% if (result) { %>
                <% searchresult.forEach(a => { %>
            <div class="weblenz-card">
                <div>
                <img class="weblenz-card-icon" src="<%= a.image %>" alt="">
                <a href="<%= a.link %>"><h2 class="weblenz-card-title"><%= a.title %></h2></a>
                </div>
                <p class="weblenz-card-text">
                    <%= a.description %>
                </p>
            </div>
            <% }) %>
            <% } %>
            <!-- Repeat card structure 3 more times -->
        </div>
    </div>
    <footer class="weblenz-footer">
        <p>Copyright WebLens 2024. All right reserved</p>
    </footer>
</body>
</html>
